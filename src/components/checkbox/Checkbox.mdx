import { Source } from '@storybook/addon-docs/blocks';
import { packageName } from '@data/variables.ts';


# Checkbox

Checkboxes allow users to select one, none, or several options in the user-interface. They can be used:

- Independently 
- In checkbox lists
 
## Implementation

#### Import component
<Source
  language="tsx"
  code={
    `import { Checkbox } from '${packageName}'`}
/>

#### Checkbox object
<Source
  language="tsx"
  code={
    `import { Checkbox } from '{packageName}'`}
/>

#### Basic configuration
<Source
  language="tsx"
  code={
    `import { Checkbox } from '{packageName}'`}
/>

#### Visual representation

## Required properties

## Optional properties

## API reference



